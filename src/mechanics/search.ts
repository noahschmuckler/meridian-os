// Mini-search affordance: every bubble has a collapsible search button that summons primitives.
// Phase 0 stub — registry-driven matching comes in Phase 3.

import type { SearchResult } from '../types';

export function searchPrimitives(_query: string): SearchResult[] {
  return [];
}
